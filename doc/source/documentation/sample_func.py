def func(arg1, arg2):
    """Summary line <should be only one line>.

    Extended description of the function. The extended description, 
	which can span multiple lines, should provide a general overview 
	of the function.

    .. warning::
       Use the ``.. warning::`` directive within the docstring for any
       warnings that need to be explicitly state. For example, you 
       want to include a warning if the method is to be deprecated in 
	   the next release.

    Parameters
    ----------
    arg1 : int
        Description of arg1.

    arg2 : str
        Description of arg2.

    Returns
    -------
    bool
        Description of the return value.

    Examples
    --------
    >>> func(1, 'foo')
    True

    """
    return True
